{
  "name": "bnb-issuer",
  "private": true,
  "version": "1.0.0",
  "description": "Deploys a reserve-backed wrapped CAC BEP-20 on BNB Smart Chain and seeds an initial CAC/USDT PancakeSwap V2 pool.",
  "scripts": {
    "compile": "hardhat compile",
    "deploy": "hardhat run scripts/deploy.js --network bsc",
    "seed-pool": "hardhat run scripts/create_pool_and_seed.js --network bsc"
  },
  "devDependencies": {
    "hardhat": "^2.22.0",
    "@nomicfoundation/hardhat-toolbox": "^5.0.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.0",
    "dotenv": "^16.4.0",
    "ethers": "^6.13.0"
  }
}
